# Robots.txt for YOD Elazig - اتحاد الطلاب اليمنيين
# Website: https://yodelazig.org

# =====================================================================
# Default rules for all crawlers
# =====================================================================
User-agent: *

# Allow full access by default
Allow: /

# Disallow crawling of admin, API, and Next.js internal routes
Disallow: /admin/
Disallow: /api/
Disallow: /_next/
Disallow: /login

# Disallow crawling of private/member-only pages for all languages
Disallow: /ar/access
Disallow: /en/access
Disallow: /tr/access
Disallow: /ar/archive
Disallow: /en/archive
Disallow: /tr/archive

# =====================================================================
# Rules for specific crawlers (if any special handling is needed)
# =====================================================================

# Googlebot uses the default rules. Crawl rate is managed in Google Search Console.
User-agent: Googlebot

# Bingbot
User-agent: Bingbot
Disallow: /admin/
Disallow: /api/
Disallow: /_next/
Disallow: /login
Disallow: /ar/access
Disallow: /en/access
Disallow: /tr/access
Disallow: /ar/archive
Disallow: /en/archive
Disallow: /tr/archive

# =====================================================================
# Sitemap location
# =====================================================================
Sitemap: https://yodelazig.org/sitemap.xml

